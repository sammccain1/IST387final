#install.packages('quanteda')
#install.packages('quanteda.textplots')
#install.packages('quanteda.textstats')
library(quanteda)
library(quanteda.textplots)
library(quanteda.textstats)
str(project_dataset_omit)
DescriptionCorpus <- corpus(project_dataset_omit$Description)
toks <- tokens(DescriptionCorpus, remove_punct = TRUE)
toks_nostop <- tokens_select(toks, pattern = stopwords("en"), selection = "remove")
Description_dfm <- dfm(toks_nostop)
textplot_wordcloud(Description_dfm, min_count = 1000)
textstat_frequency(Description_dfm, 5)
# # The two most frequently used words were "trash" and "skipped." This indicates that a large portion of service reports had to do with people's trash pickup being skipped, which seems to be a common problem.

```{r}
install.packages("quanteda")
install.packages("quanteda.textplots")
install.packages("quanteda.textstats")
library(quanteda)
library(quanteda.textplots)
library(quanteda.textstats)
str(project_dataset_omit)
DescriptionCorpus <- corpus(project_dataset_omit$Description)
toks <- tokens(DescriptionCorpus, remove_punct = TRUE)
toks_nonstop <- tokens_select(toks, pattern = stopwords("en", selection = "remove")
                              description_dfm <- dfm(toks_nonstop)
                              textplot_wordcloud(Description_dfm, min_count = 1000)
                              textstat_frequency(Description_dfm, 5)
```{r}
